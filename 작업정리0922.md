# 작업 정리 — 2026-09-22

Please-YE (KITTI 2D 객체 검출 → Hailo-10H 배포) 프로젝트의 **지금까지 한 일**과 **다음 과제**를 한 장으로 정리한 문서입니다.
세부 실행 절차는 `SETUP_LOCAL.md`, 원래 계획은 `README.md`, 다른 PC에서 온 코드의 반영 내역은 `upstream/README.md`를 보세요.

---

## 1. 한눈에 보는 현재 상태

| 항목 | 상태 |
|---|---|
| 데이터 파이프라인 (다운로드 → 추출 → 감사 → 분할 → YOLO 변환) | **완료** (PC00, 2026-09-17) |
| 분할 무결성 (누수 0) | **검증 완료** — 21개 항목 ALL PASS |
| AP40 평가기 (MMDetection3D `fe25f7a` 고정) | **설치·검증 완료** — perfect 100.0 / empty 0.0 |
| Baseline 학습 (YOLO11n, 1248×384, 100 epoch) | **완료** — internal_val Moderate AP40 **95.59** |
| 파라미터 / FLOPs / Latency 측정 | 스크립트만 확보 (`benchmark_model.py`, 직사각형 미지원) — **미실행** |
| INT8 양자화 파이프라인 | **없음** ← 예선 배점의 절반 |
| ONNX / HEF export | **없음** |
| exact3 vs neighbors3 결정 | **미결** (데이터는 둘 다 준비됨) |
| 예선 보고서 마감 | **2026-11-01 23:59** (D-40) |

---

## 2. 지금까지 한 일 (시간순)

### 2-1. 다른 PC (RTX A6000 48GB ×2) — 09-11 ~ 09-14, youngeunlee · looksambrook

- 레포 생성, 공모전 공지 문서 2건 업로드
- 1단계 파이프라인 구축: `prepare_kitti.py`(ZIP CRC 검증·추출), `audit_kitti.py`, `make_splits.py`(대표성 최적화 분할), `convert_kitti_to_yolo.py`(junction 기반, 이미지 복사 없음), `evaluate_kitti.py`(AP40), `verify_evaluator.py`, `smoke_train.py`
- **고정 분할 확정**: train 5,481 / internal_val 1,000 / calibration 1,024(⊂ train) / eval_val 1,000(공식 채점셋, 학습 사용 금지)
- 검증 스택 고정: PyTorch 2.6.0+cu124, Ultralytics 8.4.108, numpy 2.4.6, Pillow 11.0.0, PyYAML 6.0.2

### 2-2. PC00 (RTX 3080 10GB ×1) — 09-17, 이 폴더에서 Claude와 진행

**개발 PC가 A6000 → 3080으로 바뀌면서** 경로·배치·Python 환경을 전부 이 PC에 맞게 다시 잡았습니다.

| 시각 | 커밋 | 내용 |
|---|---|---|
| 16:44 | `3565e72` | PC00용 세팅 스크립트 추가: `check_env.ps1`, `download_kitti.ps1`, `run_prepare.ps1`, `verify_prepared.py`, `SETUP_LOCAL.md`. 원본 스크립트는 무수정 |
| 17:23 | `8d77aa2` | 공지 하드 제약 정리(가로 ≤1280, 32 배수, 직사각형 권장, Moderate AP40, 누수 즉시 실격). `find_batch_size.py`로 **이 GPU의 배치 상한 실측**. KITTI 다운로드·추출·감사·변환·평가기 검증 전부 완료 |
| 20:39 | `a77e5fa` | `train_baseline.py`, `predict_kitti.py` 추가. **Baseline 학습 완료 및 결과 기록** |

**환경 결정 사항**

- 데이터는 `D:\datasets\KITTI` (C: 여유 20.7GB < 필요 24GB). venv·체크포인트는 C:
- Python 3.13.2 `.venv` (PATH의 MSYS2 python으로는 torch 설치 불가)
- torch 2.6.0+cu124 유지 — README 검증 버전. GPU가 달라도 버전은 안 바꿈
- `workers=0` — Windows에서 dataloader 워커 8개가 access violation으로 학습을 죽임

**배치 상한 실측 (fp16 AMP, peak ≤ 7GB 기준)**

| 모델 | 1280×384 | 640×192 |
|---|---|---|
| yolo11n | 48 (최대 56) | 128 |
| yolo11s | 24 (최대 28) | 96 |
| yolo11m | 12 (최대 14) | 48 |

GPU epoch 시간은 0.3~1.4분으로 연산은 병목이 아니고, D:(HDD) 디스크 I/O가 병목.

**Baseline 결과 (`runs/baseline/yolo11n_exact3_1248`)**

| 항목 | 값 |
|---|---|
| 모델 | YOLO11n, COCO 사전학습 → nc=3, 2,582,737 params, 5.26MB |
| 해상도 | 1248×384 (rect). imgsz 1280은 val이 1312로 튀어 공지 한도 초과 → 1248 선택 |
| 학습 | 100 epoch, batch 32, AdamW auto, seed 20260917, 2.88시간 |
| **Moderate AP40 (internal_val)** | **Car 98.08 / Pedestrian 93.70 / Cyclist 94.99 → 평균 95.59** |
| Easy / Hard 평균 | 96.63 / 93.95 |

주의: ① best epoch = 99/100 → **미수렴**. ② `rect=True`가 mosaic·mixup·cutmix를 **강제 비활성화** (로그엔 mosaic=1.0으로 찍히지만 미적용).

산출물 위치 (git 제외, PC00 로컬): `runs/baseline/yolo11n_exact3_1248/weights/best.pt`, `runs/predictions/baseline_internal_val/` (1,000 파일), `runs/metrics_baseline_internal_val.json`

### 2-3. 다른 PC — 09-17 17:18, youngeunlee

- `Please-YE-baseline-v1.zip` 업로드 (커밋 `f5fb41e`). 정사각형 640 기준 baseline 스크립트 5개 + README 확장. **GPU 검증 안 됨** (`SHARING.txt`)

### 2-4. 통합 — 09-22, 이 세션

- PC00의 3개 커밋과 원격의 zip 커밋을 **merge** (충돌 없음)
- zip을 `upstream/baseline-v1/`에 풀어 보관, zip 파일은 제거 (`git show f5fb41e:Please-YE-baseline-v1.zip`로 복원 가능)
- zip에서 `benchmark_model.py`, `baseline_common.py`만 `scripts/`에 채택. 이름이 겹치는 `train_baseline.py`·`predict_kitti.py`는 **실제 결과를 낸 PC00 버전 유지** (근거는 `upstream/README.md`)
- 이 문서 작성

---

## 3. 레포 구조 (지금)

```
scripts/
  ├ 원본 8개 (무수정)      prepare / audit / make_splits / convert / evaluate / verify_evaluator / smoke_train / setup_evaluator.ps1
  ├ PC00 세팅 4개          check_env.ps1 / download_kitti.ps1 / run_prepare.ps1 / verify_prepared.py
  ├ PC00 학습 3개          find_batch_size.py / train_baseline.py / predict_kitti.py
  └ upstream 채택 2개      baseline_common.py / benchmark_model.py
splits/                    고정 분할 (계약 — 덮어쓰지 않음)
eval_val.txt               공식 채점셋 1,000장 (학습 사용 금지)
upstream/baseline-v1/      youngeunlee zip 보관본
SETUP_LOCAL.md             PC00 실행 절차 + 공지 제약 + 실측값
README.md                  원 계획 (경로는 A6000 PC 기준)
```

---

## 4. 다음 과제 (우선순위 순)

예선 배점은 **최적화 기법 25 + 성능 개선도 25 vs 정확도 유지 15**. 정확도를 더 올리는 것보다 **양자화 파이프라인과 측정**이 점수의 대부분입니다.

### P0 — 예선 제출에 반드시 필요 (없으면 제출 불가)

1. **INT8 양자화 파이프라인** — 배점의 절반. `splits/calibration.txt` 1,024장과 `calibration.local.yaml`은 준비돼 있음.
   - 1차: PyTorch/ONNX 레벨 PTQ로 Drop Rate 측정 체계 먼저 세우기 (baseline AP40 95.59 대비)
   - 2차: Hailo DFC(x86_64 Linux 전용, ARM·Windows 불가)로 `.har` → `.hef`. **Hailo-10H 전용** 컴파일 (칩 간 비호환)
   - x86_64 Linux 환경(WSL2 Ubuntu 22.04 또는 별도 PC) 확보 필요 — PC00은 Windows
2. **측정 스크립트 완성** — 파라미터 수, FLOPs, 메모리, 데스크탑 Latency는 예선 평가 항목
   - `benchmark_model.py`에 `--imgsz 1248x384` 직사각형 옵션 추가 (현재 정사각형만)
   - baseline과 양자화 모델 **둘 다** 측정하여 표로
3. **ONNX export + Hailo 미지원 레이어 사전 점검** — YOLO11n의 어떤 op이 CPU fallback을 유발하는지 확인. 필요 시 YOLOv8n으로 후퇴 판단
4. **재현 스크립트 하나로 묶기** — 학술부가 직접 재현 검증. `run_benchmark.ps1` 한 번으로 baseline·양자화 모델 평가가 돌아가야 함

### P1 — 점수를 올리는 것

5. **Baseline 재학습** — 미수렴(best=99/100) 해소. `--epochs 200` 또는 `--patience` 조정. 결과가 좋아지면 이게 새 baseline
6. **증강 A/B** — `rect=True`로 mosaic이 꺼진 상태 vs 정사각형(mosaic 활성) 비교. 어느 쪽이 AP40에 유리한지 internal_val로 판단
7. **exact3 vs neighbors3 결정** — README가 미뤄둔 항목. neighbors3 학습 1회 후 internal_val AP40 비교
8. **`cache='ram'` 적용** — 8GB면 5,481장 캐시 가능(RAM 31.7GB). HDD 병목 제거로 실험 회전 속도 향상

### P2 — 마감 전 정리

9. **eval_val.txt 최종 측정** — 모든 선택이 끝난 뒤 **단 한 번만**. 그 전엔 절대 건드리지 않음
10. **보고서** — 적용 기법 · 실험 환경(A6000→3080 이력 포함) · 측정 방법 · baseline 대비 결과
11. **README 갱신** — 현재 README는 A6000 PC 기준. SETUP_LOCAL.md 내용을 반영해 단일 진입점으로

### 협업상 정리 필요

- **zip 업로드 대신 브랜치/PR로** — zip은 diff가 안 남고 이번처럼 같은 이름 다른 내용 파일이 생김. 다른 PC 작업은 `git push origin <branch>`로
- **하드웨어·설정 이력 기록** — 어떤 PC, 어떤 배치로 돌렸는지는 `runs/*/args.yaml`에만 남고 git엔 없음. 결과를 낼 때마다 `SETUP_LOCAL.md`처럼 문서에 남기기
- Windows git에서 `.git/index.lock`이 남아 있으면 `del .git\index.lock` 후 진행

---

## 5. 절대 기준 (변경 불가)

- `eval_val.txt` 1,000장은 학습·튠·증강 탐색에 사용 금지 — **즉각 실격**
- 최종 지표는 Car/Pedestrian/Cyclist **Moderate AP40 평균**
- `splits/`는 계약 — 재생성해도 커밋본을 덮어쓰지 않음 (`run_prepare.ps1 -VerifySplits`는 비교만)
- 이미지·라벨·체크포인트·변환 산출물은 git에 올리지 않음
