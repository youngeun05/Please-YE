# 계획 점검 — 공지·규칙 대조 (2026-09-22)

GitHub `main`(5bd3c28) 전체와 두 공지 문서(`2026_모바일_하반기공모전_트랙2_최종안내.docx`, `트랙2 지정주제 벤치마크 공지.hwpx`)를 대조하고, Ultralytics 8.4.108 소스와 Hailo Model Zoo(master, DFC v5.4.0)를 직접 확인한 결과입니다.

**한 줄 결론**: 데이터·평가·baseline은 견고합니다. 문제는 *다음 단계 계획*에 있습니다 — **"양자화만 하면 된다"는 전제가 예선 배점 구조와 맞지 않고**, 데스크탑에서 INT8 이득을 *어떤 런타임으로 측정할지*가 비어 있습니다.

---

## 🔴 계획을 고쳐야 하는 것 (구조적)

### 1. 양자화만으로는 "성능 개선도 25점"의 절반이 움직이지 않는다

공지가 예선에서 보는 지표: **Moderate mAP 유지율, 파라미터 수, FLOPs, 메모리, 데스크탑 Latency**.

| 지표 | 양자화(INT8)만 했을 때 |
|---|---|
| 파라미터 수 | **변화 없음** (연산 구조 동일) |
| FLOPs | **변화 없음** |
| 모델 크기 | FP32 대비 ¼ — 단, Ultralytics `.pt`는 이미 FP16 저장(2.16 B/param)이라 `.pt` 기준으론 ½ |
| Latency | **런타임에 따라 0~3배** (아래 2번) |
| 메모리 | 런타임 의존 |

→ 파라미터·FLOPs를 줄이는 축(해상도 640×192 변형, 채널 프루닝, yolo11n→더 작은 폭)을 **하나 이상** 계획에 넣어야 5개 지표가 다 움직입니다. 현재 `작업정리0922.md` P0에는 없습니다.

참고: yolo11n FLOPs는 640² 기준 6.55G → **1248×384에서 약 7.7G, 640×192에서 약 2.0G** (픽셀 수 비례 추정).

### 2. 데스크탑 INT8 측정 런타임이 정해져 있지 않다 — 지금 도구로는 측정 불가

- `benchmark_model.py`는 **PyTorch eager FP32 forward** 전용. PyTorch eager는 CUDA INT8 conv를 실행하지 않으므로 INT8 모델의 속도·메모리 이득이 **0으로 측정**됩니다.
- INT8이 실제로 빨라지는 데스크탑 런타임: **ONNX Runtime(CPU, QDQ 모델)**, **TensorRT(INT8 엔진)**, OpenVINO. 이 중 하나로 baseline(FP32)과 최적화 모델을 **같은 런타임**에서 재야 비교가 성립합니다.
- `benchmark_model.py`는 정사각형 imgsz만 받아 1248×384를 못 잽니다.

→ 결정 필요: 심사위원이 재현 가능한 런타임 = **ONNX Runtime**(pip 한 줄, CPU/GPU 모두)이 가장 안전. TensorRT는 GPU·버전 의존성이 커서 재현 위험.

### 3. "최적화 모델 파일"이 두 종류가 되어 정확도가 갈린다

- Hailo용 `.har`/`.hef`는 **심사위원 데스크탑에서 실행 불가** (DFC 설치 필요, `.hef`는 보드 전용).
- 예선 제출은 "학술부가 직접 실행해 재현"이 조건 → 데스크탑에서 도는 INT8(ONNX QDQ)이 필요.
- 그런데 ONNX Runtime 양자화와 Hailo DFC 양자화는 **다른 알고리즘** → Drop Rate가 서로 다릅니다.

→ 보고서에 "예선 제출 최적화 모델 = ONNX INT8(데스크탑 재현용), 본선 이식용 = Hailo INT8(DFC 에뮬레이션 결과 병기)"로 **명시적으로 정의**해야 합니다. 둘 다 같은 calibration 1,024장을 쓰면 비교가 정직해집니다.

### 4. 재현성이 Windows에 묶여 있다

심사위원이 Linux에서 돌릴 가능성이 높은데:

- `predict_kitti.py` 기본 경로 `D:\datasets\KITTI`
- 데이터 준비가 PowerShell + Windows junction 전제 (`New-Item -ItemType Junction`)
- `workers=0`은 Windows 크래시 회피용 — Linux엔 불필요하지만 무해

→ `--kitti-root` 필수 인자화, Linux용 심링크 스크립트(`ln -s`), **`run_benchmark.sh`/`.ps1` 한 번으로 baseline·최적화 모델 평가가 끝나는 진입점**이 필요합니다. 재현 실패는 "감점 또는 심사 제외".

---

## 🟡 과학적·수학적으로 주의할 것

### 5. 1,000장 평가셋의 통계적 잡음 — 특히 Cyclist

`split_report.json` 기준 eval_val의 Moderate 객체 수: **Car 2,091 / Pedestrian 473 / Cyclist 138**.

이항 근사(p≈0.95)로 AP 추정 표준오차:

| 클래스 | n | ±SE (AP 점) |
|---|---:|---:|
| Car | 2,091 | ±0.5 |
| Pedestrian | 473 | ±1.0 |
| **Cyclist** | **138** | **±1.9** |
| 3클래스 평균 | — | **±0.7** |

→ A/B 실험(exact3 vs neighbors3, 증강 on/off, epoch 수)에서 **평균 AP 0.5점 차이는 잡음**입니다. Drop Rate도 마찬가지로 ±0.7점 흔들립니다. 결론을 내릴 때 **bootstrap(이미지 단위 리샘플링 1,000회) 신뢰구간**을 같이 내야 "결과 해석의 논리성" 35점 항목에서 설득력이 생깁니다. 현재 `evaluate_kitti.py`에는 없습니다.

### 6. 랜덤 분할과 근접 프레임 — AP 95.6은 과대평가지만, 대회 점수 예측엔 유효

- KITTI 7,481장은 같은 주행의 시간적으로 가까운 프레임이 인덱스 전체에 흩어져 있습니다. 랜덤 분할이면 학습셋과 거의 같은 장면이 검증셋에 들어갑니다 (학계에서 Chen split 3712/3769을 따로 만든 이유).
- `eval_val.txt`를 확인해 보니 **주최측도 균일 랜덤 추출**입니다 (인덱스 평균 간격 7.49 ≈ 7481/1000, 97%가 학습 이미지와 인접 인덱스).
- 따라서 internal_val(우리 것)과 eval_val(주최측)은 **같은 성질** → internal_val 점수가 eval_val 점수의 좋은 예측치입니다. 다만 둘 다 실제 도로 일반화 성능보다 높게 나옵니다.

→ 문제는 아니지만 보고서 "한계점" 절에 이 사실을 쓰면 됩니다. 쓰지 않으면 심사위원이 "95.6이 말이 되냐"고 물을 때 답이 없습니다.

### 7. "미수렴(best epoch 99/100)" 판단은 틀렸다 — 재학습 우선순위 낮춰야

`results.csv`: epoch 99 mAP50-95 **0.6958** → epoch 100 **0.6947**, 학습률은 4.3e-5 → 2.8e-5로 선형 감쇠 종료 구간. best가 마지막 근처에 오는 것은 **LR 스케줄이 끝나는 시점의 정상 현상**이고, 곡선은 평탄합니다.

→ `작업정리0922.md` P1-5 "200 epoch 재학습"은 **3080에서 6시간 써서 기대 이득 <0.3점(잡음 이하)**. 우선순위를 내리고, 대신 `cos_lr` 또는 lr0 조정처럼 스케줄을 바꾸는 실험이 정보량이 큽니다.

### 8. `rect=True` 부작용 — 소스 확인 결과

Ultralytics 8.4.108 소스에서 직접 확인:

- `dataset.py:310-312` — rect일 때 **mosaic·mixup·cutmix = 0 강제**. 문서 기록 맞음.
- `train.py:94` — rect일 때 shuffle을 끄지만, **batch shape이 전부 같으면 유지**. KITTI 4종 원본 크기(1242×375, 1224×370, 1238×374, 1241×376)는 imgsz 1248에서 모두 **1248×384로 동일** → shuffle은 살아 있습니다. (걱정했던 것보다 나음)
- 남는 증강: scale 0.5, translate 0.1, fliplr 0.5, HSV. 완전히 없는 게 아님.

→ "정사각형 mosaic 학습 후 직사각형 추론" A/B는 여전히 가치 있지만, 5번의 잡음 한계 안에서 판단해야 합니다.

### 9. 학습 중 검증 shape ≠ 배포 shape

rect 검증은 pad 0.5가 붙어 **1280×416**으로 돌고, `predict_kitti.py`는 **1248×384** 고정입니다. `best.pt` 선택 기준(fitness)이 배포 shape과 다른 입력에서 계산된 것. 영향은 작지만 보고서에 한 줄 필요합니다.

### 10. neighbors3 실험의 기대 효과는 수학적으로 작다

KITTI 평가 규칙(mmdet3d `clean_data`): Car 평가 시 **Van GT는 ignore** — Van을 Car로 맞혀도 TP/FP 아님, 놓쳐도 FN 아님. Person_sitting↔Pedestrian도 동일. 즉 neighbors3(Van→Car 매핑)은 **평가 지표에 직접 영향이 없고**, Car 특징 학습에 간접 영향만 있습니다.

→ 해도 되지만 5번 잡음(±0.7) 이내의 차이가 나올 확률이 높음. P1에서 뒤로.

### 11. Drop Rate 정의를 주최측에 확인

"유지율(Drop Rate)"이 `(baseline − quant) / baseline`(상대)인지 `baseline − quant`(절대)인지 공지에 없습니다. 95.6 기준으로 1점 드롭은 상대 1.05%. 보고서에는 **둘 다** 표기하는 게 안전합니다.

---

## 🟢 확인해서 안심되는 것

| 걱정했던 것 | 확인 결과 |
|---|---|
| YOLO11의 C2PSA 어텐션이 Hailo에서 CPU fallback을 일으키는가 | **아니다.** Hailo Model Zoo master가 **yolov11n을 Hailo-10H 공식 지원** (DFC v5.4.0, COCO 640² FP 39.0 → INT8 38.0, 302 FPS). 어텐션 포함 전체가 컴파일됨. NMS만 host CPU(`engine=cpu`) |
| Hailo DFC를 Windows PC00에서 못 쓰나 | **WSL2 공식 지원** (Ubuntu 22.04/24.04, Python 3.10~3.12). x86 Linux PC 따로 필요 없음 |
| 직사각형 입력 컴파일 가능한가 | Model Zoo에 `hailo_yolov8n_384_640.alls` 같은 직사각형 설정이 이미 있음 → 1248×384 가능 |
| 버전 혼동 | **Hailo-8/8L = Model Zoo v2.x + DFC 3.x**, **Hailo-10H = master + DFC 5.x**. 공지의 "칩 간 비호환" 경고와 정확히 일치. 10H는 5.x만 |
| 누수 방지 | `train_baseline.py` guard + `verify_prepared.py` 21항목 + eval_val 미접촉. 견고 |
| 평가기 | mmdet3d 커밋 고정, perfect 100.0 / empty 0.0 검증. Moderate 기준(높이>25px, occlusion≤1, truncation≤0.3), strict IoU(Car 0.7 / Ped·Cyc 0.5) 표준 그대로 |
| 해상도 규정 | 1248×384: 가로 ≤1280 ✓, 32 배수 ✓, 스케일 1.0048(원본 1:1) ✓. 공지 "정밀도 중심 1280×384"와 동등 |

추가로 발견: PC00 폴더에 `yolo26n.pt`가 받아져 있는데 커밋엔 없습니다. **YOLO26은 NMS-free**라 Hailo Model Zoo v5.3.0부터 지원 — Pi 5의 CPU NMS 병목을 없애는 진짜 최적화 카드가 될 수 있습니다. 검토 가치 있음(단, 검증 부담 증가).

---

## 수정된 우선순위 제안 (마감 11/1, D-40)

| 순위 | 할 일 | 왜 |
|---|---|---|
| **P0-1** | ONNX export(1248×384 고정) + **ONNX Runtime FP32 baseline 측정** (latency·메모리·크기) | 모든 비교의 분모. 심사위원 재현 가능 |
| **P0-2** | **ORT static INT8 PTQ** (calibration 1,024장) → internal_val AP40 + Drop Rate + **bootstrap CI** | 배점 25+15의 근거 |
| **P0-3** | **파라미터·FLOPs를 실제로 줄이는 축 1개**: 640×192 변형 학습 또는 채널 프루닝 → 같은 표에 추가 | 1번 문제 해결. 트레이드오프 곡선이 "기술적 깊이" 점수 |
| **P0-4** | WSL2에 DFC 5.4 설치 → `.har` INT8 → **DFC 에뮬레이션 AP40** (본선 이식 계획의 증거) | 공지 "예선부터 10H 전제" 강력 권장 |
| **P0-5** | `run_benchmark.{sh,ps1}` 단일 진입점 + Linux 경로 대응 + README 갱신 | 재현 실패 = 감점/제외 |
| P1 | 증강 A/B(정사각형 mosaic vs rect), cos_lr | 5번 잡음 감안해 CI로 판단 |
| P1 | YOLO26n 검토 | NMS-free의 본선 이점 |
| P2 | neighbors3, 200 epoch 재학습 | 기대 효과 잡음 이하 |
| 마지막 | **eval_val 측정 단 1회** | 규칙 |

---

## 출처

- 공지: `2026_모바일_하반기공모전_트랙2_최종안내.docx` §4.2~4.4, §6; `트랙2 지정주제 벤치마크 공지.hwpx` §2~3
- Ultralytics 8.4.108: `ultralytics/data/dataset.py:310-312`, `ultralytics/models/yolo/detect/train.py:93-96`, `ultralytics/data/base.py:370-`, `ultralytics/data/build.py:308`
- Hailo Model Zoo master: `docs/public_models/HAILO10H/HAILO10H_object_detection.rst` (yolov11n 행), `hailo_model_zoo/cfg/networks/yolov11n.yaml`, `cfg/alls/generic/yolov11n.alls`, `docs/GETTING_STARTED.rst`, `README.rst` 호환성 안내 — https://github.com/hailo-ai/hailo_model_zoo
- 레포: `splits/split_report.json`, `runs/baseline/yolo11n_exact3_1248/results.csv`, `args.yaml`
