# [트랙 2] 임베디드 On-device AI 최적화 챌린지 — 상세 해체 분석

> 지정주제: 자율주행 객체인지(Object Detection) / 3인 팀 / 목표: **1등**
> 하드웨어: Raspberry Pi 5 (8GB) + Hailo-8L(13 TOPS) 또는 Hailo-8(26 TOPS) AI HAT+ + RPi 카메라 모듈
> 작성일: 2026-08-14

---

## 0. 한 줄 요약

이 대회의 본질은 **"YOLO 계열 모델을 학습 → ONNX 변환 → Hailo Dataflow Compiler로 INT8 양자화 → HEF 컴파일 → RPi5+NPU에서 구동 → Baseline 대비 속도/메모리/정확도를 정량 증명"**하는 엔지니어링 파이프라인을 얼마나 깊고 엄밀하게 수행하느냐의 싸움이다. 모델 성능 자체보다 **최적화 전후의 개선폭과 분석의 엄밀함**이 점수다.

---

## 1. 대회 요구사항 역산 분석 (배점 → 전략)

평가 배점을 역산하면 어디에 시간을 써야 하는지가 나온다.

| 평가 항목 | 배점 | 우리가 실제로 해야 할 일 |
|---|---|---|
| 추론 성능 향상도 (Latency, FPS) | **30점** | Baseline(RPi5 CPU에서 FP32 추론) 대비 NPU INT8의 속도 개선을 수십 배 단위로 증명. 배점 1위 → 여기서 벌어진다 |
| 정확도 (mAP) | **25점** | 양자화 후 mAP 하락을 1~2%p 이내로 방어. "빠르지만 정확도 붕괴"는 감점 |
| 리소스 효율 (메모리, NPU/CPU 활용) | **20점** | 모델 크기 감소율(FP32→INT8 = 약 4배), RAM footprint, CPU 점유율 감소 측정 |
| 분석 방법론·결과 증명 | **15점** | 지정 벤치마크 준수 + 재현 가능한 측정 스크립트 + 통계적으로 타당한 실험 설계 |
| 발표·보고서 | **10점** | Before/After 표·그래프, 실시간 카메라 데모 |

**핵심 통찰 3가지:**

1. **속도(30) + 리소스(20) = 50점이 "최적화 공격력"**, 정확도(25)는 "방어력"이다. 공격적으로 양자화·경량화하되 정확도 방어 기법(후술)을 체계적으로 쌓는 팀이 이긴다.
2. 계획서에 명시된 조항: *"기본 모델이 RPi5에서 실행 불가능한 경우 PC/서버에서 비교 후 RPi 구동을 증명"* — 역으로 읽으면, **Baseline을 'RPi5 CPU에서 겨우 도는 무거운 FP32 모델'로 잡을수록 개선폭이 극적으로 보인다.** Baseline 선정 자체가 전략이다.
3. 지정 벤치마크(데이터셋·평가 프로토콜)는 오리엔테이션 전후 공지 → **어떤 데이터셋이 와도 돌아가는 범용 평가 파이프라인**(COCO 포맷 mAP 계산기)을 미리 만들어 두면 공지 즉시 선점할 수 있다. 자율주행이므로 KITTI 또는 BDD100K(혹은 그 서브셋)일 가능성이 높다.

---

## 2. 하드웨어 해체 분석

### 2.1 Raspberry Pi 5 (8GB)
- Cortex-A76 4코어 2.4GHz, PCIe 2.0 x1 레인 → AI HAT+가 이 레인으로 연결됨
- CPU만으로 YOLOv8n(640px) 추론 시 약 1.5~3 FPS 수준 → **이것이 우리의 Baseline 스토리**
- 8GB RAM은 온보드 작업(추론, 평가)에는 충분하지만 **모델 컴파일(양자화)에는 부족** → 컴파일은 별도 x86_64 리눅스 PC에서 (중요, §4.3)
- 발열: Active Cooler 필수. 스로틀링되면 측정값이 흔들리므로 벤치마크 전 `vcgencmd measure_temp`로 온도 통제

### 2.2 Hailo-8L vs Hailo-8 (AI HAT+)
| | Hailo-8L | Hailo-8 |
|---|---|---|
| 연산력 | 13 TOPS | 26 TOPS |
| YOLOv8n 참고 처리량 | ~180 FPS (batch1, 모델주 문서 기준) | ~400+ FPS |
| 지원 정밀도 | INT8 (일부 레이어 16bit 가능) | 동일 |

- **구조적 특징**: Hailo NPU는 GPU가 아니라 **데이터플로우 아키텍처**다. 모델 그래프를 컴파일 타임에 칩의 연산 자원에 정적으로 매핑한다. 그래서 반드시 전용 컴파일러(DFC)로 HEF 바이너리를 만들어야 하고, **INT8 양자화가 선택이 아니라 필수**다. → "양자화를 왜 했는가?"에 대한 답이 하드웨어 구조에서 나온다. 보고서에 이 서사를 쓰면 이해도 점수를 먹는다.
- 어느 쪽이 지급되든 파이프라인은 동일 (컴파일 시 타깃만 `hailo8l`/`hailo8`로 지정). 키트 확정 공지를 확인할 것.

### 2.3 소프트웨어 스택 (RPi5 쪽)
```
[카메라/비디오] → GStreamer + picamera2
      ↓
HailoRT (런타임, hailortcli 포함) ← HEF 파일 로드
      ↓
TAPPAS Core (hailonet 등 GStreamer 플러그인, 후처리)
      ↓
hailo-rpi5-examples (공식 예제 레포 — 검출 파이프라인 완성품 제공)
```

---

## 3. 전체 프로젝트 파이프라인 (End-to-End)

```
[Phase A: 준비]                [Phase B: 모델]              [Phase C: 최적화·컴파일]
 환경 구축(RPi+x86 PC)          모델 후보 선정                ONNX export
 데이터셋 확보(KITTI/BDD)   →   자율주행 데이터로            →  DFC parse (.har)
 평가 파이프라인 제작            파인튜닝(전이학습)              INT8 양자화(calib set)
                                Baseline 성능 측정              HEF 컴파일
                                                                  ↓
[Phase F: 산출물]              [Phase E: 분석]              [Phase D: 배포·측정]
 보고서·발표자료            ←   Before/After 정량 비교    ←   RPi5에서 HEF 구동
 실시간 카메라 데모             ablation 실험                  mAP/FPS/Latency/메모리 측정
 재현 가능한 코드 공개          그래프·표 제작                 실시간 카메라 파이프라인
```

### Phase A — 환경 구축 (1주)
1. **RPi5 세팅**: Raspberry Pi OS (Bookworm 64bit), `sudo apt install hailo-all` (HailoRT + 드라이버 + TAPPAS 일괄 설치), PCIe Gen3 활성화(`raspi-config`, 처리량 향상), Active Cooler 장착
2. **x86_64 리눅스 PC 확보** (필수!): Hailo Dataflow Compiler는 **ARM에서 안 돌아감**. Ubuntu 22.04 x86 PC(RAM 32GB 권장, 부족하면 최적화 레벨 낮춤/스왑/클라우드 VM·Colab 대안) + Hailo AI SW Suite Docker
3. hailo.ai 개발자 계정 가입 → DFC/Suite 다운로드 (무료, 가입 필요)
4. `hailo-rpi5-examples` 레포 클론 → 기본 detection 데모부터 구동해 스택 검증

### Phase B — 모델·데이터 (2~3주)
1. 자율주행 데이터셋 확보: **KITTI 2D detection** (7,481장, Car/Pedestrian/Cyclist 등 8클래스), **BDD100K** (10만 장, 주야간·악천후 포함 10클래스). 지정 벤치마크 공지 전까지 KITTI로 파이프라인을 완성해 두고, 공지 후 데이터만 교체
2. 모델 선정 (§5 상세 분석) 후 자율주행 클래스로 파인튜닝 (Ultralytics로 학습, GPU는 Colab/학교 서버 활용)
3. **Baseline 측정**: RPi5 CPU에서 PyTorch/ONNX Runtime FP32 추론 → mAP, latency, FPS, RAM, CPU% 기록. 이 수치가 모든 비교의 기준점

### Phase C — 양자화·컴파일 (2~3주, 핵심 구간)
1. `model.export(format="onnx", opset=11, imgsz=640)` — 후처리(NMS) 제외한 순수 그래프로 export
2. DFC 3단계: **parse**(ONNX→HAR) → **optimize**(INT8 양자화: calibration 이미지 64장 이상으로 activation 통계 수집) → **compile**(HEF 생성)
3. 지름길: Hailo Model Zoo의 `hailomz compile yolov8s --ckpt our_model.onnx --calib-path ./calib --classes N` 한 방 명령 + 모델별 `.alls` 스크립트 자동 적용
4. 정확도 하락 시 방어 기법 사다리 적용 (§6)

### Phase D — 배포·측정 (2주)
1. `hailortcli run model.hef --measure-latency` / `hailortcli benchmark model.hef` — HW-only FPS·latency 공식 측정
2. Python(HailoRT API) 또는 GStreamer 파이프라인으로 실제 이미지 입력 E2E 측정 (전처리+추론+후처리 포함 — HW-only 수치와 구분해서 둘 다 보고하면 방법론 점수↑)
3. 카메라 모듈로 실시간 데모 파이프라인 완성
4. 메모리: `psutil`/`smem`으로 RSS 측정, CPU 점유율: `top`/`pidstat`, (선택) USB 전력계로 전력·에너지 측정

### Phase E — 분석·Ablation (2주)
- 최소 비교 축: ① FP32 CPU Baseline ② INT8 NPU (기본 양자화) ③ INT8 NPU (정확도 복원 기법 적용) ④ (차별화) 입력 해상도/모델 크기 스윕
- 각 조건에서 mAP@0.5, mAP@0.5:0.95, latency(mean/p95), FPS, 모델 크기, RAM, CPU%, (전력) 측정 → 표+그래프

### Phase F — 산출물 (1~2주)
- 보고서(적용 기법·실험 환경·측정 방법·Baseline 대비 결과), 발표자료, 재현 스크립트(`run_benchmark.sh` 하나로 심사위원 앞 재현), 실시간 데모

---

## 4. 함정 리스트 (미리 알면 몇 주를 아끼는 것들)

1. **DFC는 x86_64 전용** — RPi에서 컴파일하려다 막히는 팀이 반드시 나온다. 우리는 처음부터 "학습·컴파일=x86 PC/클라우드, 추론·측정=RPi"로 역할을 분리한다.
2. **양자화는 RAM 먹는 하마** — 32GB 권장. 부족하면 optimization level을 낮추거나(정확도 손해) 클라우드 VM 사용.
3. **전처리 불일치로 mAP 0 나오는 버그** — 학습 시 letterbox 리사이즈를 썼는데 배포 파이프라인에서 단순 resize를 쓰면 검출이 전멸한다. 전처리(리사이즈 방식, 정규화, RGB/BGR)를 학습↔캘리브레이션↔배포 3곳에서 완전히 일치시킬 것. (실전에서 가장 흔한 삽질 1위)
4. **NMS 위치** — Hailo는 NMS를 칩 밖(CPU 후처리) 또는 컴파일된 후처리로 수행. ONNX export 시 NMS를 그래프에 포함하면 parse가 실패한다. Ultralytics 기본 export(head까지)+Hailo 후처리 조합을 쓸 것.
5. **버전 지옥** — DFC, HailoRT, 펌웨어, Model Zoo 버전은 서로 호환 매트릭스가 있다. 시작할 때 한 세트로 고정하고 팀 전체가 같은 버전 사용 (예: DFC 5.x + HailoRT 5.x).
6. **발열 스로틀링** — 측정 중 CPU 온도 80°C 넘으면 클럭이 떨어져 수치가 오염된다. 측정 로그에 온도를 같이 기록하면 방법론 점수도 챙긴다.
7. **캘리브레이션 셋 품질** — 아무 이미지 64장이 아니라, 실제 평가 데이터의 밝기·스케일·클래스 분포를 대표하는 이미지여야 한다. 주간/야간/역광이 섞인 자율주행 데이터라면 캘리브레이션 셋에도 섞어야 함.
8. **Hailo-8L용 HEF ≠ Hailo-8용 HEF** — 컴파일 타깃 확인. 키트 확정 공지 후 타깃 맞춰 재컴파일.

---

## 5. 모델 선정 심층 분석

### 5.1 후보 비교

| 모델 | 파라미터 | 장점 | 리스크 | Hailo 지원 |
|---|---|---|---|---|
| **YOLOv8n/s** | 3.2M / 11.2M | Model Zoo 공식 지원, `.alls` 최적화 스크립트·retraining docker 완비, 자료 최다 | 최신성 어필 약함 | ◎ 공식 |
| **YOLO11n/s** | 2.6M / 9.4M | v8 대비 파라미터 14%↓·FLOPs 22%↓에 정확도 동급 이상, Model Zoo 지원됨 | v8보다 자료 약간 적음 | ◎ 공식 |
| YOLO26n | ~2.4M | 2026.1 출시, edge-first 설계(NMS-free, DFL 제거, 양자화 내성) | **NMS-free E2E head가 Hailo 후처리 경로와 미스매치** — 커뮤니티 우회법 존재하나 삽질 위험 | △ 커뮤니티 |
| YOLOv10 | - | NMS-free 선구자 | 동일한 E2E export 미지원 문제 | △ |
| YOLO-NAS | - | 양자화 친화 구조(INT8 손실 최소) | 생태계 축소 중, 학습 파이프라인 번거로움 | ○ |
| SSD-MobileNet | - | 초경량 | 정확도 낮아 25점 방어 불리 | ◎ |

### 5.2 추천 전략: "주력 + 보험 + 차별화" 3단 구성

- **주력: YOLO11s** (또는 YOLOv8s) — Model Zoo 공식 지원으로 파이프라인 리스크 최소 + "최신 모델" 어필 + s 사이즈로 정확도 방어. Hailo-8L에서 s 사이즈도 실시간(30 FPS+) 충분.
- **보험: YOLOv8n** — 무조건 돌아가는 검증된 경로. 파이프라인 전체를 먼저 n으로 관통시켜 두고, 주력 모델을 교체 투입.
- **차별화(여력 시): YOLO26n 도전기** — "최신 edge-first 모델을 Hailo에 올리며 겪은 E2E head 호환 문제와 해결"은 그 자체로 보고서의 훌륭한 분석 챕터가 된다. 실패해도 분석 점수로 회수 가능.

**핵심 원칙: 파이프라인을 먼저 관통시키고 모델을 갈아끼운다.** 처음부터 최신 모델로 시작하면 어디서 막혔는지조차 모른다.

### 5.3 Baseline 설계 (전략적으로 중요)

평가 30점이 "Baseline 대비 향상도"이므로 Baseline 정의가 곧 점수다. 권장 구성:

- **공식 Baseline**: 동일 모델(YOLO11s) FP32를 RPi5 **CPU**에서 ONNX Runtime으로 추론 → 예상 2~4 FPS
- **최적화 후**: 동일 모델 INT8 HEF를 NPU에서 추론 → 예상 수십~수백 FPS
- 결과 서사: "동일 모델·동일 데이터에서 **수십 배 속도 향상 + 모델 크기 1/4 + CPU 점유율 수분의 1, mAP 하락은 1%p 이내**"
- 심사 질문 "GPU와 비교는?"에 대비해 PC GPU 수치도 참고로 측정해 두면 좋음 (계획서의 'PC/서버 비교' 조항과도 부합)

---

## 6. 양자화·최적화 기법 사다리 (정확도 방어 25점의 핵심)

Hailo DFC의 최적화는 `.alls` 모델 스크립트로 제어한다. 아래를 **사다리 순서대로** 적용하며 각 단계의 mAP를 기록하면 그 자체가 ablation 표가 된다.

| 단계 | 기법 | 내용 | 비용 |
|---|---|---|---|
| 0 | 기본 PTQ | calibration 64장+, per-channel INT8 | 몇 분 |
| 1 | Calibration 셋 개선 | 대표성 있는 512~1024장으로 확대 | 몇 분 |
| 2 | Equalization | 채널 간 weight 범위 균등화 (full-precision 단계 기법) | 자동 |
| 3 | Activation/Weight Clipping | outlier 클리핑으로 양자화 범위 낭비 제거 | 스크립트 1줄 |
| 4 | **출력 레이어 16-bit** | `quantization_param(output_layer, precision_mode=a16_w16)` — detection head는 양자화에 민감하므로 효과 큼 | 소폭 속도 손해 |
| 5 | AdaRound | 레이어별 weight 반올림 학습 (opt level 상향) | 시간·RAM↑ |
| 6 | AdaQuant / Finetune (QAT급) | 소량 데이터로 양자화 인지 미세조정 | GPU 필요 |

**추가 차별화 기법 (여기까지 가면 우승권):**
- **구조적 프루닝 → 재학습 → 양자화**: 채널 프루닝으로 FLOPs 자체를 줄인 뒤 양자화. "양자화만 한 팀"과 격차를 벌리는 지점
- **지식 증류(KD)**: YOLO11m(teacher) → YOLO11s(student)로 파인튜닝 시 증류 적용, 작은 모델의 mAP를 끌어올려 정확도 방어
- **입력 해상도 스윕**: 640 vs 512 vs 416 — 자율주행 원거리 소형 객체(보행자) recall과 FPS의 트레이드오프 곡선 제시. "우리는 곡선 위에서 운영점을 선택했다"는 서사는 방법론 15점 직결
- **멀티스트림/배치 처리량**: `hailortcli run2`로 스트림 수를 늘려 최대 처리량 측정 — NPU 활용 효율(리소스 20점) 증거

---

## 7. 측정 방법론 (재현성 15점 설계)

### 7.1 측정 도구 및 지표

| 지표 | 도구 | 비고 |
|---|---|---|
| HW latency / FPS | `hailortcli run model.hef --measure-latency`, `hailortcli benchmark` | 공식 수치, JSON 저장 옵션 |
| E2E latency | Python 스크립트(전처리+추론+후처리 타이머, 1000회, warmup 50회 제외) | mean / p50 / p95 / p99 보고 |
| mAP | pycocotools (COCO 포맷 통일) | 지정 벤치마크 프로토콜 공지 시 즉시 교체 |
| 모델 크기 | 파일 크기 (.pt/.onnx vs .hef) | FP32→INT8 ≈ 1/4 |
| RAM | `psutil` RSS, 추론 전/중/후 | Baseline과 동일 조건 |
| CPU 점유율 | `pidstat -p <pid> 1` | NPU 오프로딩 효과 증명 |
| 전력(선택) | USB-C 인라인 전력계 | J/inference 계산 시 강한 차별화 |
| 온도 | `vcgencmd measure_temp` 로깅 | 스로틀링 통제 증명 |

### 7.2 재현성 체크리스트
- 모든 측정은 스크립트화 (`benchmark.py` + `run_all.sh`) → 심사위원 앞에서 원커맨드 재현
- 고정: 랜덤 시드, 입력 해상도, confidence/IoU threshold, HailoRT·DFC 버전, OS 버전 → 보고서 부록에 명시
- 각 실험 3회 반복 평균 ± 표준편차 표기
- Git 레포에 학습 설정(yaml), `.alls` 스크립트, 캘리브레이션 셋 목록까지 커밋

---

## 8. 11주 로드맵 & 3인 역할 분담

일정: 8월 중순(키트 지급) → 11월 초(최종 발표), 약 11주.

| 주차 | 마일스톤 |
|---|---|
| 1~2주 | 환경 구축(RPi+x86), 공식 예제 데모 구동, KITTI/BDD 다운로드, 평가 파이프라인 골격 |
| 3~4주 | YOLOv8n으로 전체 파이프라인 관통 (학습→ONNX→HEF→RPi 구동→mAP 측정까지 1회전) |
| 5~6주 | 주력 모델(YOLO11s) 파인튜닝, Baseline 정밀 측정, 지정 벤치마크 공지 반영 |
| 7~8주 | 양자화 사다리 단계별 적용 + ablation, (여력 시) 프루닝/증류/YOLO26 실험 |
| 9주 | 실시간 카메라 데모 완성, 멀티스트림·해상도 스윕, 전력 측정 |
| 10주 | 전체 결과 정리, 그래프·표 제작, 보고서 초안 |
| 11주 | 보고서 완성, 발표 자료·리허설, 재현 스크립트 최종 점검 |

**역할 분담 (3인):**

- **A. 모델·학습 담당**: 데이터셋 전처리·파인튜닝·증류/프루닝 실험, ONNX export. GPU 환경(Colab/서버) 운영
- **B. 최적화·컴파일 담당**: x86 PC에서 DFC 운영, `.alls` 스크립트 튜닝, 양자화 사다리 실험, HEF 산출. Hailo 커뮤니티/문서 리서치
- **C. 배포·측정 담당(팀장 권장)**: RPi5 환경·카메라 파이프라인, 벤치마크 스크립트 개발, 측정 데이터 관리, 보고서·발표 총괄

※ 1~4주차(파이프라인 관통기)는 3인이 함께 붙고, 5주차부터 분업. 매주 30분 스탠드업으로 "이번 주 수치"를 공유 — 모든 의사결정은 수치로.

---

## 9. 1등 차별화 전략 요약

경쟁 팀 대부분은 "모델 zoo에서 YOLO 받아 기본 양자화 → FPS 좋아졌다"에서 끝난다. 우리가 벌리는 지점:

1. **양자화 사다리 ablation** — 기법을 하나씩 쌓으며 mAP 회복 과정을 표로 증명 (분석 15점 + 정확도 25점)
2. **양자화 너머의 최적화** — 프루닝·증류로 FLOPs 자체를 감축 (속도 30점 상한 돌파)
3. **운영점 선택의 서사** — 해상도-FPS-mAP 트레이드오프 곡선에서 자율주행 요구사항(예: 30 FPS, 보행자 recall 우선)에 맞는 점을 근거 있게 선택
4. **에너지 관점** — J/inference까지 측정하는 팀은 거의 없다 (리소스 20점 차별화)
5. **완벽한 재현성** — 원커맨드 벤치마크 + 버전 고정 문서화
6. **실시간 카메라 데모** — 발표장에서 라이브로 도는 검출 화면은 10점 만점 직행

---

## 10. 시작 전 준비 체크리스트 (지금 당장)

- [ ] hailo.ai 개발자 계정 가입 (3인 전원) → SW Suite 다운로드 권한 확보
- [ ] x86_64 Ubuntu PC 확보 (개인 PC 듀얼부팅 / WSL2는 비권장·네이티브 권장 / 학교 서버 / 클라우드)
- [ ] 학습용 GPU 확보 (Colab Pro or 학과 서버)
- [ ] KITTI·BDD100K 계정 가입 및 다운로드 시작 (BDD100K는 용량 큼)
- [ ] GitHub 팀 레포 생성 (코드·실험 로그·측정 데이터 버전 관리)
- [ ] `hailo-rpi5-examples`, `hailo_model_zoo` 레포 정독 (특히 `docs/OPTIMIZATION.rst`, `basic-pipelines.md`)
- [ ] 키트 스펙 확정 공지 확인 (8L인지 8인지 → 컴파일 타깃 결정)
- [ ] 지정 벤치마크 공지 나오면 즉시 평가 프로토콜 분석

---

## 11. 핵심 참고 자료

**공식 문서·레포**
- [hailo-ai/hailo-rpi5-examples](https://github.com/hailo-ai/hailo-rpi5-examples) — RPi5 공식 예제 (설치·파이프라인)
- [hailo-ai/hailo_model_zoo](https://github.com/hailo-ai/hailo_model_zoo) — 모델 zoo, `hailomz compile`, [OPTIMIZATION.rst](https://github.com/hailo-ai/hailo_model_zoo/blob/master/docs/OPTIMIZATION.rst) (양자화 기법 문서)
- [Model Zoo 커스텀 데이터셋 재학습 가이드](https://github.com/hailo-ai/hailo_model_zoo/blob/master/docs/RETRAIN_ON_CUSTOM_DATASET.rst)
- [Ultralytics Hailo Export 공식 문서](https://docs.ultralytics.com/integrations/hailo) — YOLO26 지원 범위 명시

**실전 튜토리얼**
- [커스텀 YOLOv8 → HEF 전체 가이드 (Medium, 2026.6)](https://mrkp.medium.com/run-your-own-yolov8-model-on-raspberry-pi-5-hailo-ai-hat-complete-pt-to-hef-guide-1a9dfa0d1140)
- [ONNX → Hailo8L 변환 단계별 가이드 (RidgeRun)](https://www.ridgerun.ai/post/convert-onnx-model-to-hailo8l)
- [커스텀 데이터셋 + Docker 실전기 (Towards AI)](https://pub.towardsai.net/custom-dataset-with-hailo-ai-hat-yolo-raspberry-pi-5-and-docker-0d88ef5eb70f)
- [Seeed Studio: RPi5 멀티스트림 벤치마크](https://wiki.seeedstudio.com/benchmark_of_multistream_inference_on_raspberrypi5_with_hailo8/)
- [hailortcli 사용법 (Macnica)](https://www.macnica.co.jp/en/business/semiconductor/articles/hailo/145098/)

**모델·연구 동향**
- [YOLO26 아키텍처 분석 (arXiv)](https://arxiv.org/pdf/2509.25164) / [YOLO26 NMS-free 분석](https://arxiv.org/html/2601.12882v2)
- [YOLO26 → Hailo 컴파일 커뮤니티 스레드](https://community.hailo.ai/t/yolo26-compilation-to-hef/18660) / [커뮤니티 우회 레포](https://github.com/DanielDubinsky/yolo26_hailo)
- [자율주행 edge 검출 경량화 연구 (2025, YOLOv5+attention, KITTI/BDD)](https://pmc.ncbi.nlm.nih.gov/articles/PMC12387144/)
- [2026 객체검출 모델 총정리 (Roboflow)](https://blog.roboflow.com/best-object-detection-models/)
- [16-bit 출력 레이어 설정 (Hailo Community)](https://community.hailo.ai/t/16-bit-quantization-on-final-layers/2292)
